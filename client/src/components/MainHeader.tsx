import { LogIn, LogOut, Menu, UserRound } from "lucide-react";

type User = { name: string; role: "visitor" | "admin" } | null;

export function MainHeader({ user, onNavigate, onLogin, onAdmin, onLogout, onMenu }: { user: User; onNavigate: (id: string) => void; onLogin: () => void; onAdmin: () => void; onLogout: () => void; onMenu: () => void }) {
  const initial = user?.name.trim().charAt(0).toUpperCase() || "";
  return <><aside className="fixed left-0 top-1/2 z-50 hidden -translate-y-1/2 border-y border-r border-primary/25 bg-[#10110f]/95 py-4 shadow-2xl backdrop-blur-xl lg:block"><div className="flex flex-col gap-3"><button onClick={user?.role === "admin" ? onAdmin : user ? onLogout : onLogin} className="group flex flex-col items-center gap-2 px-3 text-[10px] text-white/70 transition hover:text-primary"><span className="grid h-10 w-10 place-items-center rounded-full border border-primary/45 bg-primary/10 font-[family-name:var(--font-display)] text-lg text-primary">{user ? initial : <LogIn className="h-4 w-4" />}</span><span>{user ? user.name.split(" ")[0] : "تسجيل دخول"}</span></button>{user && <button onClick={onLogout} className="flex flex-col items-center gap-1 px-3 text-[10px] text-white/35 transition hover:text-primary"><LogOut className="h-4 w-4" />خروج</button>}</div></aside><header className="fixed inset-x-0 top-0 z-40 border-b border-white/[.06] bg-black/50 backdrop-blur-xl"><div className="mx-auto flex h-20 max-w-7xl items-center justify-between px-5 lg:px-8"><button onClick={() => onNavigate("home")} className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center border border-primary/55 font-[family-name:var(--font-display)] text-xl text-primary">P</span><span className="hidden font-[family-name:var(--font-display)] text-xl sm:block">PRESTIGE <span className="text-primary">ESTATES</span></span></button><nav className="hidden gap-8 text-sm text-white/75 lg:flex"><button onClick={() => onNavigate("home")}>الرئيسية</button><button onClick={() => onNavigate("properties")}>العقارات</button><button onClick={() => onNavigate("collections")}>المجموعات</button><button onClick={() => onNavigate("services")}>الخدمات</button><button onClick={() => onNavigate("contact")}>تواصل</button></nav><div className="flex items-center gap-3">{user && <button onClick={user.role === "admin" ? onAdmin : undefined} title={user.name} className="grid h-10 w-10 place-items-center rounded-full border border-primary/45 bg-primary/10 font-[family-name:var(--font-display)] text-lg text-primary">{initial}</button>}<button onClick={onMenu} className="lg:hidden" aria-label="القائمة"><Menu /></button></div></div></header></>;
}
